// Count the number of properties.
let student = {
      name: 'Ariana Grande',
      age: 21,
      city: 'Gaibandha',
      isStudent: true
  };
  const keys = Object.keys(student);
  console.log("number:", keys.length);
  console.log("property:",);
  keys.forEach(key =>
  {console.log(key);}
  );
